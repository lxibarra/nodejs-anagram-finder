{
  "name": "anagrams",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -R app.spec.js && cat coverage/lcov.info | node_modules/coveralls/bin/coveralls.js && rm -rf coverage"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^3.4.1",
    "mocha": "^3.0.2"
  },
  "devDependencies": {
    "coveralls": "^2.11.12",
    "mocha-lcov-reporter": "^1.2.0"
  }
}
